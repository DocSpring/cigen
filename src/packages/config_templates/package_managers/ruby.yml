versions:
  - ruby
  - bundler
detect:
  - name: bundler
    lockfile: Gemfile.lock
    command: bundle install --deployment
checksum_sources:
  - Gemfile
  - Gemfile.lock
cache_paths:
  - vendor/bundle
  - .bundle
