{
  "name": "cigen",
  "version": "0.1.0",
  "description": "A blazing-fast Rust CLI that compiles template-based definitions into complete CI pipelines",
  "private": true,
  "scripts": {
    "nx": "nx",
    "format": "ultracite format && prettier --write '**/*.{yml,yaml,md}'",
    "format:check": "ultracite check && prettier --check '**/*.{yml,yaml,md}'"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "lefthook": "^1.12.2",
    "nx": "21.2.2",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "ultracite": "5.0.32"
  },
  "workspaces": [
    "."
  ]
}
