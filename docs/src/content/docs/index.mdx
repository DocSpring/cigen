---
title: Cigen - Universal CI Configuration Generator
description: Write your CI pipeline configuration once and deploy it anywhere. Cigen generates provider-specific configurations from universal templates.
template: splash
hero:
  tagline: Declarative CI that compiles to native YAML
  image:
    file: ../../assets/splash-image.jpeg
  actions:
    - text: Quick Start
      link: /cigen/quick-start/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/DocSpring/cigen
      icon: external
---

import { CardGrid } from '@astrojs/starlight/components';
import CardWithIcon from '../../components/CardWithIcon.astro';
import {
  Package,
  Layers,
  Rocket,
  Puzzle,
  Laptop,
  ShieldCheck,
} from 'lucide-astro';

Define your pipeline once in a concise, provider‑agnostic format.
CIGen validates it with rich error spans and emits provider‑native configuration
(CircleCI today; others next).

## Why Cigen?

<CardGrid>
  <CardWithIcon title="Provider‑native output" iconComponent={Puzzle}>
    Author once; ship native configs. CIGen compiles your universal spec into
    production‑ready CircleCI YAML without leaking abstractions.
  </CardWithIcon>
  <CardWithIcon title="Strong validation" iconComponent={ShieldCheck}>
    Schema + data‑level checks with precise error spans. Unknown fields are
    preserved; invalid states fail fast with actionable messages.
  </CardWithIcon>
  <CardWithIcon
    title="Zero‑boilerplate checkout & cache"
    iconComponent={Rocket}
  >
    Git checkout is optimized (shallow fetch, tag/branch controls, host key
    scanning). Cache steps are injected automatically when you declare them.
  </CardWithIcon>
  <CardWithIcon title="Package install detection" iconComponent={Package}>
    Detects pnpm/yarn/npm/bun, bundler, and pip/pipenv; adds the right install
    steps with provider‑compatible flags and de‑duplicated execution.
  </CardWithIcon>
  <CardWithIcon title="Multi‑arch variants" iconComponent={Laptop}>
    Generate amd64/arm64 job variants from a single job definition.
  </CardWithIcon>
  <CardWithIcon title="Split config at scale" iconComponent={Layers}>
    Keep large configs maintainable. Merge files from{' '}
    <code>.cigen/config/*</code>
    automatically and reuse file groups, jobs, and templates. Split your
    workflows and jobs into multiple files — CIGen merges them seamlessly.
  </CardWithIcon>
</CardGrid>

## Principles

- Validate early: schema + semantics with rich spans
- Fail fast: explicit, actionable errors; no silent defaults
- Preserve unknowns: never drop user‑provided data
- Native first: emit provider‑idiomatic configuration
- Minimal boilerplate: declare intent; CIGen injects mechanics

## Quick Example

Define your pipeline structure:

```
.cigen/
├── cigen.yml                    # Provider and global config
├── config/
│   └── source_file_groups.yml  # Reusable file patterns
├── workflows/
│   └── main/
│       └── jobs/
│           ├── test.yml         # Test job definition
│           └── build.yml        # Build job definition
```

```yaml title=".cigen/config.yml"
provider: circleci
```

```yaml title=".cigen/config/source_file_groups.yml"
source_file_groups:
  nodejs:
    - 'src/**/*.js'
    - 'package.json'
    - 'package-lock.json'
```

```yaml title=".cigen/workflows/main/jobs/test.yml"
image: cimg/node:18.0
source_files: '@nodejs'
packages: node
steps:
  - run: npm test
```

```yaml title=".cigen/workflows/main/jobs/build.yml"
image: cimg/node:18.0
architectures: [amd64, arm64]
requires: [test]
source_files: '@nodejs'
packages: node
steps:
  - run: npm run build
```

Generate CircleCI configuration:

```bash
cigen generate
```

Results in a `.circleci/config.yml` with:

- Git checkout
- Optional package installation steps (npm/yarn/pnpm/bun, bundler, etc.)
- Separate jobs for each architecture (`build_amd64`, `build_arm64`)

## How It Works

1. Describe workflows, jobs, packages, and caches in `.cigen/`
2. CIGen validates and compiles to provider‑native YAML
3. Your CI runs faster and cleaner with less duplication

## Current Status

- CircleCI: supported
- Other providers: planned

## Getting Started

1. [Install cigen](/cigen/installation/)
2. Follow the [Quick Start guide](/cigen/quick-start/)
3. Read about cigen's [philosophy](/cigen/philosophy/)
4. Explore [configuration options](/cigen/configuration/overview/)
