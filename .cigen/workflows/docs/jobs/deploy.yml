image: ubuntu-latest

requires:
  - build

environment:
  name: github-pages
  url: https://docspring.github.io/cigen/

steps:
  - name: Deploy to GitHub Pages
    id: deployment
    uses: actions/deploy-pages@v4
