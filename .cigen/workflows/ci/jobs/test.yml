image: rust:latest
source_files:
  - "@rust"
  - "@tests"
  - "@examples"

steps:
  - run:
      name: Build workspace
      command: cargo build --workspace --all-targets
  - run:
      name: Run tests
      command: cargo test --all-features
