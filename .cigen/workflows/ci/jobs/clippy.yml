image: rust:latest
source_files: "@rust"

steps:
  - run:
      name: Install clippy
      command: rustup component add clippy
  - run:
      name: Clippy check
      command: cargo clippy --all-targets --all-features -- -D warnings
